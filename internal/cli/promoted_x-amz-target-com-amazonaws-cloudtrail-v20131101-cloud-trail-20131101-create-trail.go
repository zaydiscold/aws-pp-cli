// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetComAmazonawsCloudtrailV20131101CloudTrail20131101CreateTrailPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyCloudWatchLogsLogGroupArn string
	var bodyCloudWatchLogsRoleArn string
	var bodyEnableLogFileValidation bool
	var bodyIncludeGlobalServiceEvents bool
	var bodyIsMultiRegionTrail bool
	var bodyIsOrganizationTrail bool
	var bodyKmsKeyId string
	var bodyName string
	var bodyS3BucketName string
	var bodyS3KeyPrefix string
	var bodySnsTopicName string
	var bodyTagsList string

	cmd := &cobra.Command{
		Use:         "x-amz-target-com-amazonaws-cloudtrail-v20131101-cloud-trail-20131101-create-trail",
		Short:       "Creates a trail that specifies the settings for delivery of log data to an Amazon S3 bucket.",
		Long:        "Shortcut for 'x-amz-target-com-amazonaws-cloudtrail-v20131101-cloud-trail-20131101-create-trail create-trail'. Creates a trail that specifies the settings for delivery of log data to an Amazon S3 bucket.",
		Example:     "  aws-pp-pp-cli x-amz-target-com-amazonaws-cloudtrail-v20131101-cloud-trail-20131101-create-trail --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-com-amazonaws-cloudtrail-v20131101-cloud-trail-20131101-create-trail.create-trail", "pp:method": "POST", "pp:path": "http://cloudtrail.us-east-1.amazonaws.com/#X-Amz-Target=com.amazonaws.cloudtrail.v20131101.CloudTrail_20131101.CreateTrail"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "name")
			}
			if !cmd.Flags().Changed("s3-bucket-name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "s3-bucket-name")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://cloudtrail.us-east-1.amazonaws.com/#X-Amz-Target=com.amazonaws.cloudtrail.v20131101.CloudTrail_20131101.CreateTrail"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyCloudWatchLogsLogGroupArn != "" {
				body["CloudWatchLogsLogGroupArn"] = bodyCloudWatchLogsLogGroupArn
			}
			if bodyCloudWatchLogsRoleArn != "" {
				body["CloudWatchLogsRoleArn"] = bodyCloudWatchLogsRoleArn
			}
			if bodyEnableLogFileValidation != false {
				body["EnableLogFileValidation"] = bodyEnableLogFileValidation
			}
			if bodyIncludeGlobalServiceEvents != false {
				body["IncludeGlobalServiceEvents"] = bodyIncludeGlobalServiceEvents
			}
			if bodyIsMultiRegionTrail != false {
				body["IsMultiRegionTrail"] = bodyIsMultiRegionTrail
			}
			if bodyIsOrganizationTrail != false {
				body["IsOrganizationTrail"] = bodyIsOrganizationTrail
			}
			if bodyKmsKeyId != "" {
				body["KmsKeyId"] = bodyKmsKeyId
			}
			if bodyName != "" {
				body["Name"] = bodyName
			}
			if bodyS3BucketName != "" {
				body["S3BucketName"] = bodyS3BucketName
			}
			if bodyS3KeyPrefix != "" {
				body["S3KeyPrefix"] = bodyS3KeyPrefix
			}
			if bodySnsTopicName != "" {
				body["SnsTopicName"] = bodySnsTopicName
			}
			if bodyTagsList != "" {
				var parsedTagsList any
				if err := json.Unmarshal([]byte(bodyTagsList), &parsedTagsList); err != nil {
					return fmt.Errorf("parsing --tags-list JSON: %w", err)
				}
				body["TagsList"] = parsedTagsList
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyCloudWatchLogsLogGroupArn, "cloud-watch-logs-log-group-arn", "", "Cloud watch logs log group arn")
	cmd.Flags().StringVar(&bodyCloudWatchLogsRoleArn, "cloud-watch-logs-role-arn", "", "Cloud watch logs role arn")
	cmd.Flags().BoolVar(&bodyEnableLogFileValidation, "enable-log-file-validation", false, "Enable log file validation")
	cmd.Flags().BoolVar(&bodyIncludeGlobalServiceEvents, "include-global-service-events", false, "Include global service events")
	cmd.Flags().BoolVar(&bodyIsMultiRegionTrail, "is-multi-region-trail", false, "Is multi region trail")
	cmd.Flags().BoolVar(&bodyIsOrganizationTrail, "is-organization-trail", false, "Is organization trail")
	cmd.Flags().StringVar(&bodyKmsKeyId, "kms-key-id", "", "Kms key id")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name")
	cmd.Flags().StringVar(&bodyS3BucketName, "s3-bucket-name", "", "S3 bucket name")
	cmd.Flags().StringVar(&bodyS3KeyPrefix, "s3-key-prefix", "", "S3 key prefix")
	cmd.Flags().StringVar(&bodySnsTopicName, "sns-topic-name", "", "Sns topic name")
	cmd.Flags().StringVar(&bodyTagsList, "tags-list", "", "A list of tags.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
