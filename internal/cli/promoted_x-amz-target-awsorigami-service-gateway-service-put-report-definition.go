// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetAwsorigamiServiceGatewayServicePutReportDefinitionPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyReportDefinitionAdditionalArtifacts string
	var bodyReportDefinitionAdditionalSchemaElements string
	var bodyReportDefinitionBillingViewArn string
	var bodyReportDefinitionCompression string
	var bodyReportDefinitionFormat string
	var bodyReportDefinitionRefreshClosedReports bool
	var bodyReportDefinitionReportName string
	var bodyReportDefinitionReportVersioning string
	var bodyReportDefinitionS3Bucket string
	var bodyReportDefinitionS3Prefix string
	var bodyReportDefinitionS3Region string
	var bodyReportDefinitionTimeUnit string

	cmd := &cobra.Command{
		Use:         "x-amz-target-awsorigami-service-gateway-service-put-report-definition",
		Short:       "Creates a new report using the description that you provide.",
		Long:        "Shortcut for 'x-amz-target-awsorigami-service-gateway-service-put-report-definition put-report-definition'. Creates a new report using the description that you provide.",
		Example:     "  aws-pp-pp-cli x-amz-target-awsorigami-service-gateway-service-put-report-definition",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-awsorigami-service-gateway-service-put-report-definition.put-report-definition", "pp:method": "POST", "pp:path": "http://cur.us-east-1.amazonaws.com/#X-Amz-Target=AWSOrigamiServiceGatewayService.PutReportDefinition"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("report-definition-additional-schema-elements") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "report-definition-additional-schema-elements")
			}
			if !cmd.Flags().Changed("report-definition-compression") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "report-definition-compression")
			}
			if !cmd.Flags().Changed("report-definition-format") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "report-definition-format")
			}
			if !cmd.Flags().Changed("report-definition-report-name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "report-definition-report-name")
			}
			if !cmd.Flags().Changed("report-definition-s3-bucket") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "report-definition-s3-bucket")
			}
			if !cmd.Flags().Changed("report-definition-s3-prefix") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "report-definition-s3-prefix")
			}
			if !cmd.Flags().Changed("report-definition-s3-region") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "report-definition-s3-region")
			}
			if !cmd.Flags().Changed("report-definition-time-unit") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "report-definition-time-unit")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://cur.us-east-1.amazonaws.com/#X-Amz-Target=AWSOrigamiServiceGatewayService.PutReportDefinition"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			{
				nestedReportDefinition := map[string]any{}
				if bodyReportDefinitionAdditionalArtifacts != "" {
					var parsedReportDefinitionAdditionalArtifacts any
					if err := json.Unmarshal([]byte(bodyReportDefinitionAdditionalArtifacts), &parsedReportDefinitionAdditionalArtifacts); err != nil {
						return fmt.Errorf("parsing --report-definition-additional-artifacts JSON: %w", err)
					}
					nestedReportDefinition["AdditionalArtifacts"] = parsedReportDefinitionAdditionalArtifacts
				}
				if bodyReportDefinitionAdditionalSchemaElements != "" {
					var parsedReportDefinitionAdditionalSchemaElements any
					if err := json.Unmarshal([]byte(bodyReportDefinitionAdditionalSchemaElements), &parsedReportDefinitionAdditionalSchemaElements); err != nil {
						return fmt.Errorf("parsing --report-definition-additional-schema-elements JSON: %w", err)
					}
					nestedReportDefinition["AdditionalSchemaElements"] = parsedReportDefinitionAdditionalSchemaElements
				}
				if bodyReportDefinitionBillingViewArn != "" {
					nestedReportDefinition["BillingViewArn"] = bodyReportDefinitionBillingViewArn
				}
				if bodyReportDefinitionCompression != "" {
					nestedReportDefinition["Compression"] = bodyReportDefinitionCompression
				}
				if bodyReportDefinitionFormat != "" {
					nestedReportDefinition["Format"] = bodyReportDefinitionFormat
				}
				if bodyReportDefinitionRefreshClosedReports != false {
					nestedReportDefinition["RefreshClosedReports"] = bodyReportDefinitionRefreshClosedReports
				}
				if bodyReportDefinitionReportName != "" {
					nestedReportDefinition["ReportName"] = bodyReportDefinitionReportName
				}
				if bodyReportDefinitionReportVersioning != "" {
					nestedReportDefinition["ReportVersioning"] = bodyReportDefinitionReportVersioning
				}
				if bodyReportDefinitionS3Bucket != "" {
					nestedReportDefinition["S3Bucket"] = bodyReportDefinitionS3Bucket
				}
				if bodyReportDefinitionS3Prefix != "" {
					nestedReportDefinition["S3Prefix"] = bodyReportDefinitionS3Prefix
				}
				if bodyReportDefinitionS3Region != "" {
					nestedReportDefinition["S3Region"] = bodyReportDefinitionS3Region
				}
				if bodyReportDefinitionTimeUnit != "" {
					nestedReportDefinition["TimeUnit"] = bodyReportDefinitionTimeUnit
				}
				if len(nestedReportDefinition) > 0 {
					body["ReportDefinition"] = nestedReportDefinition
				}
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyReportDefinitionAdditionalArtifacts, "report-definition-additional-artifacts", "", "A list of additional artifacts.")
	cmd.Flags().StringVar(&bodyReportDefinitionAdditionalSchemaElements, "report-definition-additional-schema-elements", "", "A list of strings that indicate the content that is included in the report, such as service or usage type.")
	cmd.Flags().StringVar(&bodyReportDefinitionBillingViewArn, "report-definition-billing-view-arn", "", "Billing view arn")
	cmd.Flags().StringVar(&bodyReportDefinitionCompression, "report-definition-compression", "", "The compression format that AWS uses for the report.")
	cmd.Flags().StringVar(&bodyReportDefinitionFormat, "report-definition-format", "", "The format that AWS saves the report in.")
	cmd.Flags().BoolVar(&bodyReportDefinitionRefreshClosedReports, "report-definition-refresh-closed-reports", false, "Refresh closed reports")
	cmd.Flags().StringVar(&bodyReportDefinitionReportName, "report-definition-report-name", "", "The name of the report that you want to create. The name must be unique, is case sensitive, and can't include spaces.")
	cmd.Flags().StringVar(&bodyReportDefinitionReportVersioning, "report-definition-report-versioning", "", "Report versioning")
	cmd.Flags().StringVar(&bodyReportDefinitionS3Bucket, "report-definition-s3-bucket", "", "The S3 bucket where AWS delivers the report.")
	cmd.Flags().StringVar(&bodyReportDefinitionS3Prefix, "report-definition-s3-prefix", "", "The prefix that AWS adds to the report name when AWS delivers the report. Your prefix can't include spaces.")
	cmd.Flags().StringVar(&bodyReportDefinitionS3Region, "report-definition-s3-region", "", "The region of the S3 bucket that AWS delivers the report into.")
	cmd.Flags().StringVar(&bodyReportDefinitionTimeUnit, "report-definition-time-unit", "", "The length of time covered by the report.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
