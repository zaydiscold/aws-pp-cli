// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetStarlingDoveServicePutExternalEvaluationPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyConfigRuleName string
	var bodyExternalEvaluationAnnotation string
	var bodyExternalEvaluationComplianceResourceId string
	var bodyExternalEvaluationComplianceResourceType string
	var bodyExternalEvaluationComplianceType string
	var bodyExternalEvaluationOrderingTimestamp string

	cmd := &cobra.Command{
		Use:         "x-amz-target-starling-dove-service-put-external-evaluation",
		Short:       "Add or updates the evaluations for process checks. This API checks if the rule is a process check when the name of...",
		Long:        "Shortcut for 'x-amz-target-starling-dove-service-put-external-evaluation put-external-evaluation'. Add or updates the evaluations for process checks. This API checks if the rule is a process check when the name of...",
		Example:     "  aws-pp-pp-cli x-amz-target-starling-dove-service-put-external-evaluation --config-rule-name example-resource",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-starling-dove-service-put-external-evaluation.put-external-evaluation", "pp:method": "POST", "pp:path": "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.PutExternalEvaluation"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("config-rule-name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "config-rule-name")
			}
			if !cmd.Flags().Changed("external-evaluation-compliance-resource-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "external-evaluation-compliance-resource-id")
			}
			if !cmd.Flags().Changed("external-evaluation-compliance-resource-type") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "external-evaluation-compliance-resource-type")
			}
			if !cmd.Flags().Changed("external-evaluation-compliance-type") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "external-evaluation-compliance-type")
			}
			if !cmd.Flags().Changed("external-evaluation-ordering-timestamp") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "external-evaluation-ordering-timestamp")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.PutExternalEvaluation"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyConfigRuleName != "" {
				body["ConfigRuleName"] = bodyConfigRuleName
			}
			{
				nestedExternalEvaluation := map[string]any{}
				if bodyExternalEvaluationAnnotation != "" {
					nestedExternalEvaluation["Annotation"] = bodyExternalEvaluationAnnotation
				}
				if bodyExternalEvaluationComplianceResourceId != "" {
					nestedExternalEvaluation["ComplianceResourceId"] = bodyExternalEvaluationComplianceResourceId
				}
				if bodyExternalEvaluationComplianceResourceType != "" {
					nestedExternalEvaluation["ComplianceResourceType"] = bodyExternalEvaluationComplianceResourceType
				}
				if bodyExternalEvaluationComplianceType != "" {
					nestedExternalEvaluation["ComplianceType"] = bodyExternalEvaluationComplianceType
				}
				if bodyExternalEvaluationOrderingTimestamp != "" {
					nestedExternalEvaluation["OrderingTimestamp"] = bodyExternalEvaluationOrderingTimestamp
				}
				if len(nestedExternalEvaluation) > 0 {
					body["ExternalEvaluation"] = nestedExternalEvaluation
				}
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyConfigRuleName, "config-rule-name", "", "Config rule name")
	cmd.Flags().StringVar(&bodyExternalEvaluationAnnotation, "external-evaluation-annotation", "", "Annotation")
	cmd.Flags().StringVar(&bodyExternalEvaluationComplianceResourceId, "external-evaluation-compliance-resource-id", "", "Compliance resource id")
	cmd.Flags().StringVar(&bodyExternalEvaluationComplianceResourceType, "external-evaluation-compliance-resource-type", "", "Compliance resource type")
	cmd.Flags().StringVar(&bodyExternalEvaluationComplianceType, "external-evaluation-compliance-type", "", "Compliance type")
	cmd.Flags().StringVar(&bodyExternalEvaluationOrderingTimestamp, "external-evaluation-ordering-timestamp", "", "Ordering timestamp")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
