// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetStarlingDoveServiceGetAggregateResourceConfigPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyConfigurationAggregatorName string
	var bodyResourceIdentifierResourceId string
	var bodyResourceIdentifierResourceName string
	var bodyResourceIdentifierResourceType string
	var bodyResourceIdentifierSourceAccountId string
	var bodyResourceIdentifierSourceRegion string

	cmd := &cobra.Command{
		Use:         "x-amz-target-starling-dove-service-get-aggregate-resource-config",
		Short:       "Returns configuration item that is aggregated for your specific resource in a specific source account and region.",
		Long:        "Shortcut for 'x-amz-target-starling-dove-service-get-aggregate-resource-config get-aggregate-resource-config'. Returns configuration item that is aggregated for your specific resource in a specific source account and region.",
		Example:     "  aws-pp-pp-cli x-amz-target-starling-dove-service-get-aggregate-resource-config --configuration-aggregator-name example-resource",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-starling-dove-service-get-aggregate-resource-config.get-aggregate-resource-config", "pp:method": "POST", "pp:path": "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.GetAggregateResourceConfig"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("configuration-aggregator-name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "configuration-aggregator-name")
			}
			if !cmd.Flags().Changed("resource-identifier-resource-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "resource-identifier-resource-id")
			}
			if !cmd.Flags().Changed("resource-identifier-resource-type") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "resource-identifier-resource-type")
			}
			if !cmd.Flags().Changed("resource-identifier-source-account-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "resource-identifier-source-account-id")
			}
			if !cmd.Flags().Changed("resource-identifier-source-region") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "resource-identifier-source-region")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.GetAggregateResourceConfig"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyConfigurationAggregatorName != "" {
				body["ConfigurationAggregatorName"] = bodyConfigurationAggregatorName
			}
			{
				nestedResourceIdentifier := map[string]any{}
				if bodyResourceIdentifierResourceId != "" {
					nestedResourceIdentifier["ResourceId"] = bodyResourceIdentifierResourceId
				}
				if bodyResourceIdentifierResourceName != "" {
					nestedResourceIdentifier["ResourceName"] = bodyResourceIdentifierResourceName
				}
				if bodyResourceIdentifierResourceType != "" {
					nestedResourceIdentifier["ResourceType"] = bodyResourceIdentifierResourceType
				}
				if bodyResourceIdentifierSourceAccountId != "" {
					nestedResourceIdentifier["SourceAccountId"] = bodyResourceIdentifierSourceAccountId
				}
				if bodyResourceIdentifierSourceRegion != "" {
					nestedResourceIdentifier["SourceRegion"] = bodyResourceIdentifierSourceRegion
				}
				if len(nestedResourceIdentifier) > 0 {
					body["ResourceIdentifier"] = nestedResourceIdentifier
				}
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyConfigurationAggregatorName, "configuration-aggregator-name", "", "Configuration aggregator name")
	cmd.Flags().StringVar(&bodyResourceIdentifierResourceId, "resource-identifier-resource-id", "", "Resource id")
	cmd.Flags().StringVar(&bodyResourceIdentifierResourceName, "resource-identifier-resource-name", "", "Resource name")
	cmd.Flags().StringVar(&bodyResourceIdentifierResourceType, "resource-identifier-resource-type", "", "Resource type")
	cmd.Flags().StringVar(&bodyResourceIdentifierSourceAccountId, "resource-identifier-source-account-id", "", "Source account id")
	cmd.Flags().StringVar(&bodyResourceIdentifierSourceRegion, "resource-identifier-source-region", "", "Source region")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
