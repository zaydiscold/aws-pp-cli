// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetComputeOptimizerServiceGetEc2RecommendationProjectedMetricsPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyEndTime string
	var bodyInstanceArn string
	var bodyPeriod int
	var bodyRecommendationPreferencesCpuVendorArchitectures string
	var bodyStartTime string
	var bodyStat string

	cmd := &cobra.Command{
		Use:         "x-amz-target-compute-optimizer-service-get-ec2-recommendation-projected-metrics",
		Short:       "<p>Returns the projected utilization metrics of Amazon EC2 instance recommendations.</p> <note> <p>The...",
		Long:        "Shortcut for 'x-amz-target-compute-optimizer-service-get-ec2-recommendation-projected-metrics get-ec2-recommendation-projected-metrics'. <p>Returns the projected utilization metrics of Amazon EC2 instance recommendations.</p> <note> <p>The...",
		Example:     "  aws-pp-pp-cli x-amz-target-compute-optimizer-service-get-ec2-recommendation-projected-metrics --end-time 2026-01-15T09:00:00Z",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-compute-optimizer-service-get-ec2-recommendation-projected-metrics.get-ec2-recommendation-projected-metrics", "pp:method": "POST", "pp:path": "http://compute-optimizer.us-east-1.amazonaws.com/#X-Amz-Target=ComputeOptimizerService.GetEC2RecommendationProjectedMetrics"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("end-time") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "end-time")
			}
			if !cmd.Flags().Changed("instance-arn") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "instance-arn")
			}
			if !cmd.Flags().Changed("period") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "period")
			}
			if !cmd.Flags().Changed("start-time") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "start-time")
			}
			if !cmd.Flags().Changed("stat") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "stat")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://compute-optimizer.us-east-1.amazonaws.com/#X-Amz-Target=ComputeOptimizerService.GetEC2RecommendationProjectedMetrics"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyEndTime != "" {
				body["endTime"] = bodyEndTime
			}
			if bodyInstanceArn != "" {
				body["instanceArn"] = bodyInstanceArn
			}
			if bodyPeriod != 0 {
				body["period"] = bodyPeriod
			}
			{
				nestedRecommendationPreferences := map[string]any{}
				if bodyRecommendationPreferencesCpuVendorArchitectures != "" {
					var parsedRecommendationPreferencesCpuVendorArchitectures any
					if err := json.Unmarshal([]byte(bodyRecommendationPreferencesCpuVendorArchitectures), &parsedRecommendationPreferencesCpuVendorArchitectures); err != nil {
						return fmt.Errorf("parsing --recommendation-preferences-cpu-vendor-architectures JSON: %w", err)
					}
					nestedRecommendationPreferences["cpuVendorArchitectures"] = parsedRecommendationPreferencesCpuVendorArchitectures
				}
				if len(nestedRecommendationPreferences) > 0 {
					body["recommendationPreferences"] = nestedRecommendationPreferences
				}
			}
			if bodyStartTime != "" {
				body["startTime"] = bodyStartTime
			}
			if bodyStat != "" {
				body["stat"] = bodyStat
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyEndTime, "end-time", "", "End time")
	cmd.Flags().StringVar(&bodyInstanceArn, "instance-arn", "", "Instance arn")
	cmd.Flags().IntVar(&bodyPeriod, "period", 0, "Period")
	cmd.Flags().StringVar(&bodyRecommendationPreferencesCpuVendorArchitectures, "recommendation-preferences-cpu-vendor-architectures", "", "Cpu vendor architectures")
	cmd.Flags().StringVar(&bodyStartTime, "start-time", "", "Start time")
	cmd.Flags().StringVar(&bodyStat, "stat", "", "Stat")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
