// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetStarlingDoveServiceGetComplianceDetailsByResourcePromotedCmd(flags *rootFlags) *cobra.Command {
	var flagNextToken string
	var bodyComplianceTypes string
	var bodyResourceEvaluationId string
	var bodyResourceId string
	var bodyResourceType string

	cmd := &cobra.Command{
		Use:         "x-amz-target-starling-dove-service-get-compliance-details-by-resource",
		Short:       "Returns the evaluation results for the specified Amazon Web Services resource. The results indicate which Config...",
		Long:        "Shortcut for 'x-amz-target-starling-dove-service-get-compliance-details-by-resource get-compliance-details-by-resource'. Returns the evaluation results for the specified Amazon Web Services resource. The results indicate which Config...",
		Example:     "  aws-pp-pp-cli x-amz-target-starling-dove-service-get-compliance-details-by-resource",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-starling-dove-service-get-compliance-details-by-resource.get-compliance-details-by-resource", "pp:method": "POST", "pp:path": "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.GetComplianceDetailsByResource"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.GetComplianceDetailsByResource"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyComplianceTypes != "" {
				var parsedComplianceTypes any
				if err := json.Unmarshal([]byte(bodyComplianceTypes), &parsedComplianceTypes); err != nil {
					return fmt.Errorf("parsing --compliance-types JSON: %w", err)
				}
				body["ComplianceTypes"] = parsedComplianceTypes
			}
			if bodyResourceEvaluationId != "" {
				body["ResourceEvaluationId"] = bodyResourceEvaluationId
			}
			if bodyResourceId != "" {
				body["ResourceId"] = bodyResourceId
			}
			if bodyResourceType != "" {
				body["ResourceType"] = bodyResourceType
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagNextToken, "next-token", "", "Pagination token")
	cmd.Flags().StringVar(&bodyComplianceTypes, "compliance-types", "", "Compliance types")
	cmd.Flags().StringVar(&bodyResourceEvaluationId, "resource-evaluation-id", "", "Resource evaluation id")
	cmd.Flags().StringVar(&bodyResourceId, "resource-id", "", "Resource id")
	cmd.Flags().StringVar(&bodyResourceType, "resource-type", "", "Resource type")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
