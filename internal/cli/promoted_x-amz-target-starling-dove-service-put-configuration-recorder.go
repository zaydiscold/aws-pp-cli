// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetStarlingDoveServicePutConfigurationRecorderPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyConfigurationRecorderName string
	var bodyConfigurationRecorderRecordingGroupAllSupported bool
	var bodyConfigurationRecorderRecordingGroupIncludeGlobalResourceTypes bool
	var bodyConfigurationRecorderRecordingGroupResourceTypes string
	var bodyConfigurationRecorderRoleARN string

	cmd := &cobra.Command{
		Use:         "x-amz-target-starling-dove-service-put-configuration-recorder",
		Short:       "<p>Creates a new configuration recorder to record the selected resource configurations.</p> <p>You can use this...",
		Long:        "Shortcut for 'x-amz-target-starling-dove-service-put-configuration-recorder put-configuration-recorder'. <p>Creates a new configuration recorder to record the selected resource configurations.</p> <p>You can use this...",
		Example:     "  aws-pp-pp-cli x-amz-target-starling-dove-service-put-configuration-recorder",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-starling-dove-service-put-configuration-recorder.put-configuration-recorder", "pp:method": "POST", "pp:path": "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.PutConfigurationRecorder"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.PutConfigurationRecorder"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			{
				nestedConfigurationRecorder := map[string]any{}
				if bodyConfigurationRecorderName != "" {
					nestedConfigurationRecorder["name"] = bodyConfigurationRecorderName
				}
				{
					nestedConfigurationRecorderRecordingGroup := map[string]any{}
					if bodyConfigurationRecorderRecordingGroupAllSupported != false {
						nestedConfigurationRecorderRecordingGroup["allSupported"] = bodyConfigurationRecorderRecordingGroupAllSupported
					}
					if bodyConfigurationRecorderRecordingGroupIncludeGlobalResourceTypes != false {
						nestedConfigurationRecorderRecordingGroup["includeGlobalResourceTypes"] = bodyConfigurationRecorderRecordingGroupIncludeGlobalResourceTypes
					}
					if bodyConfigurationRecorderRecordingGroupResourceTypes != "" {
						var parsedConfigurationRecorderRecordingGroupResourceTypes any
						if err := json.Unmarshal([]byte(bodyConfigurationRecorderRecordingGroupResourceTypes), &parsedConfigurationRecorderRecordingGroupResourceTypes); err != nil {
							return fmt.Errorf("parsing --configuration-recorder-recording-group-resource-types JSON: %w", err)
						}
						nestedConfigurationRecorderRecordingGroup["resourceTypes"] = parsedConfigurationRecorderRecordingGroupResourceTypes
					}
					if len(nestedConfigurationRecorderRecordingGroup) > 0 {
						nestedConfigurationRecorder["recordingGroup"] = nestedConfigurationRecorderRecordingGroup
					}
				}
				if bodyConfigurationRecorderRoleARN != "" {
					nestedConfigurationRecorder["roleARN"] = bodyConfigurationRecorderRoleARN
				}
				if len(nestedConfigurationRecorder) > 0 {
					body["ConfigurationRecorder"] = nestedConfigurationRecorder
				}
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyConfigurationRecorderName, "configuration-recorder-name", "", "Name")
	cmd.Flags().BoolVar(&bodyConfigurationRecorderRecordingGroupAllSupported, "configuration-recorder-recording-group-all-supported", false, "All supported")
	cmd.Flags().BoolVar(&bodyConfigurationRecorderRecordingGroupIncludeGlobalResourceTypes, "configuration-recorder-recording-group-include-global-resource-types", false, "Include global resource types")
	cmd.Flags().StringVar(&bodyConfigurationRecorderRecordingGroupResourceTypes, "configuration-recorder-recording-group-resource-types", "", "Resource types")
	cmd.Flags().StringVar(&bodyConfigurationRecorderRoleARN, "configuration-recorder-role-arn", "", "Role arn")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
