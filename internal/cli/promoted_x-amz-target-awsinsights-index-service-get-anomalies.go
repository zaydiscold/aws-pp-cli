// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetAwsinsightsIndexServiceGetAnomaliesPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyDateIntervalEndDate string
	var bodyDateIntervalStartDate string
	var bodyFeedback string
	var bodyMaxResults int
	var bodyMonitorArn string
	var bodyNextPageToken string
	var bodyTotalImpactEndValue float64
	var bodyTotalImpactNumericOperator string
	var bodyTotalImpactStartValue float64

	cmd := &cobra.Command{
		Use:         "x-amz-target-awsinsights-index-service-get-anomalies",
		Short:       "Retrieves all of the cost anomalies detected on your account during the time period that's specified by the...",
		Long:        "Shortcut for 'x-amz-target-awsinsights-index-service-get-anomalies get-anomalies'. Retrieves all of the cost anomalies detected on your account during the time period that's specified by the...",
		Example:     "  aws-pp-pp-cli x-amz-target-awsinsights-index-service-get-anomalies",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-awsinsights-index-service-get-anomalies.get-anomalies", "pp:method": "POST", "pp:path": "http://ce.us-east-1.amazonaws.com/#X-Amz-Target=AWSInsightsIndexService.GetAnomalies"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("date-interval-start-date") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "date-interval-start-date")
			}
			if !cmd.Flags().Changed("total-impact-numeric-operator") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "total-impact-numeric-operator")
			}
			if !cmd.Flags().Changed("total-impact-start-value") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "total-impact-start-value")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://ce.us-east-1.amazonaws.com/#X-Amz-Target=AWSInsightsIndexService.GetAnomalies"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			{
				nestedDateInterval := map[string]any{}
				if bodyDateIntervalEndDate != "" {
					nestedDateInterval["EndDate"] = bodyDateIntervalEndDate
				}
				if bodyDateIntervalStartDate != "" {
					nestedDateInterval["StartDate"] = bodyDateIntervalStartDate
				}
				if len(nestedDateInterval) > 0 {
					body["DateInterval"] = nestedDateInterval
				}
			}
			if bodyFeedback != "" {
				body["Feedback"] = bodyFeedback
			}
			if bodyMaxResults != 0 {
				body["MaxResults"] = bodyMaxResults
			}
			if bodyMonitorArn != "" {
				body["MonitorArn"] = bodyMonitorArn
			}
			if bodyNextPageToken != "" {
				body["NextPageToken"] = bodyNextPageToken
			}
			{
				nestedTotalImpact := map[string]any{}
				if bodyTotalImpactEndValue != 0.0 {
					nestedTotalImpact["EndValue"] = bodyTotalImpactEndValue
				}
				if bodyTotalImpactNumericOperator != "" {
					nestedTotalImpact["NumericOperator"] = bodyTotalImpactNumericOperator
				}
				if bodyTotalImpactStartValue != 0.0 {
					nestedTotalImpact["StartValue"] = bodyTotalImpactStartValue
				}
				if len(nestedTotalImpact) > 0 {
					body["TotalImpact"] = nestedTotalImpact
				}
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyDateIntervalEndDate, "date-interval-end-date", "", "End date")
	cmd.Flags().StringVar(&bodyDateIntervalStartDate, "date-interval-start-date", "", "Start date")
	cmd.Flags().StringVar(&bodyFeedback, "feedback", "", "Feedback")
	cmd.Flags().IntVar(&bodyMaxResults, "max-results", 0, "Max results")
	cmd.Flags().StringVar(&bodyMonitorArn, "monitor-arn", "", "Monitor arn")
	cmd.Flags().StringVar(&bodyNextPageToken, "next-page-token", "", "Next page token")
	cmd.Flags().Float64Var(&bodyTotalImpactEndValue, "total-impact-end-value", 0.0, "End value")
	cmd.Flags().StringVar(&bodyTotalImpactNumericOperator, "total-impact-numeric-operator", "", "Numeric operator")
	cmd.Flags().Float64Var(&bodyTotalImpactStartValue, "total-impact-start-value", 0.0, "Start value")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
