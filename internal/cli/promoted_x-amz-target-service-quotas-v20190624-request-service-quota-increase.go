// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetServiceQuotasV20190624RequestServiceQuotaIncreasePromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyDesiredValue float64
	var bodyQuotaCode string
	var bodyServiceCode string

	cmd := &cobra.Command{
		Use:         "x-amz-target-service-quotas-v20190624-request-service-quota-increase",
		Short:       "Submits a quota increase request for the specified quota.",
		Long:        "Shortcut for 'x-amz-target-service-quotas-v20190624-request-service-quota-increase request-service-quota-increase'. Submits a quota increase request for the specified quota.",
		Example:     "  aws-pp-pp-cli x-amz-target-service-quotas-v20190624-request-service-quota-increase --quota-code example-value",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-service-quotas-v20190624-request-service-quota-increase.request-service-quota-increase", "pp:method": "POST", "pp:path": "/#X-Amz-Target=ServiceQuotasV20190624.RequestServiceQuotaIncrease"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("desired-value") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "desired-value")
			}
			if !cmd.Flags().Changed("quota-code") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "quota-code")
			}
			if !cmd.Flags().Changed("service-code") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "service-code")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/#X-Amz-Target=ServiceQuotasV20190624.RequestServiceQuotaIncrease"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyDesiredValue != 0.0 {
				body["DesiredValue"] = bodyDesiredValue
			}
			if bodyQuotaCode != "" {
				body["QuotaCode"] = bodyQuotaCode
			}
			if bodyServiceCode != "" {
				body["ServiceCode"] = bodyServiceCode
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().Float64Var(&bodyDesiredValue, "desired-value", 0.0, "Desired value")
	cmd.Flags().StringVar(&bodyQuotaCode, "quota-code", "", "Quota code")
	cmd.Flags().StringVar(&bodyServiceCode, "service-code", "", "Service code")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
