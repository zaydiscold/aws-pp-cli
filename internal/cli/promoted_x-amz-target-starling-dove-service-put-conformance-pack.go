// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetStarlingDoveServicePutConformancePackPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyConformancePackInputParameters string
	var bodyConformancePackName string
	var bodyDeliveryS3Bucket string
	var bodyDeliveryS3KeyPrefix string
	var bodyTemplateBody string
	var bodyTemplateS3Uri string
	var bodyTemplateSSMDocumentDetailsDocumentName string
	var bodyTemplateSSMDocumentDetailsDocumentVersion string

	cmd := &cobra.Command{
		Use:         "x-amz-target-starling-dove-service-put-conformance-pack",
		Short:       "<p>Creates or updates a conformance pack. A conformance pack is a collection of Config rules that can be easily...",
		Long:        "Shortcut for 'x-amz-target-starling-dove-service-put-conformance-pack put-conformance-pack'. <p>Creates or updates a conformance pack. A conformance pack is a collection of Config rules that can be easily...",
		Example:     "  aws-pp-pp-cli x-amz-target-starling-dove-service-put-conformance-pack --conformance-pack-name example-resource",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-starling-dove-service-put-conformance-pack.put-conformance-pack", "pp:method": "POST", "pp:path": "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.PutConformancePack"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("conformance-pack-name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "conformance-pack-name")
			}
			if !cmd.Flags().Changed("template-ssm-document-details-document-name") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "template-ssm-document-details-document-name")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.PutConformancePack"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyConformancePackInputParameters != "" {
				var parsedConformancePackInputParameters any
				if err := json.Unmarshal([]byte(bodyConformancePackInputParameters), &parsedConformancePackInputParameters); err != nil {
					return fmt.Errorf("parsing --conformance-pack-input-parameters JSON: %w", err)
				}
				body["ConformancePackInputParameters"] = parsedConformancePackInputParameters
			}
			if bodyConformancePackName != "" {
				body["ConformancePackName"] = bodyConformancePackName
			}
			if bodyDeliveryS3Bucket != "" {
				body["DeliveryS3Bucket"] = bodyDeliveryS3Bucket
			}
			if bodyDeliveryS3KeyPrefix != "" {
				body["DeliveryS3KeyPrefix"] = bodyDeliveryS3KeyPrefix
			}
			if bodyTemplateBody != "" {
				body["TemplateBody"] = bodyTemplateBody
			}
			if bodyTemplateS3Uri != "" {
				body["TemplateS3Uri"] = bodyTemplateS3Uri
			}
			{
				nestedTemplateSSMDocumentDetails := map[string]any{}
				if bodyTemplateSSMDocumentDetailsDocumentName != "" {
					nestedTemplateSSMDocumentDetails["DocumentName"] = bodyTemplateSSMDocumentDetailsDocumentName
				}
				if bodyTemplateSSMDocumentDetailsDocumentVersion != "" {
					nestedTemplateSSMDocumentDetails["DocumentVersion"] = bodyTemplateSSMDocumentDetailsDocumentVersion
				}
				if len(nestedTemplateSSMDocumentDetails) > 0 {
					body["TemplateSSMDocumentDetails"] = nestedTemplateSSMDocumentDetails
				}
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyConformancePackInputParameters, "conformance-pack-input-parameters", "", "Conformance pack input parameters")
	cmd.Flags().StringVar(&bodyConformancePackName, "conformance-pack-name", "", "Conformance pack name")
	cmd.Flags().StringVar(&bodyDeliveryS3Bucket, "delivery-s3-bucket", "", "Delivery s3 bucket")
	cmd.Flags().StringVar(&bodyDeliveryS3KeyPrefix, "delivery-s3-key-prefix", "", "Delivery s3 key prefix")
	cmd.Flags().StringVar(&bodyTemplateBody, "template-body", "", "Template body")
	cmd.Flags().StringVar(&bodyTemplateS3Uri, "template-s3-uri", "", "Template s3 uri")
	cmd.Flags().StringVar(&bodyTemplateSSMDocumentDetailsDocumentName, "template-ssm-document-details-document-name", "", "Document name")
	cmd.Flags().StringVar(&bodyTemplateSSMDocumentDetailsDocumentVersion, "template-ssm-document-details-document-version", "", "Document version")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
