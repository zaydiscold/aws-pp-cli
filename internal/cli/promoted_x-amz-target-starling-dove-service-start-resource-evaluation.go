// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetStarlingDoveServiceStartResourceEvaluationPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyClientToken string
	var bodyEvaluationContextEvaluationContextIdentifier string
	var bodyEvaluationMode string
	var bodyEvaluationTimeout int
	var bodyResourceDetailsResourceConfiguration string
	var bodyResourceDetailsResourceConfigurationSchemaType string
	var bodyResourceDetailsResourceId string
	var bodyResourceDetailsResourceType string

	cmd := &cobra.Command{
		Use:         "x-amz-target-starling-dove-service-start-resource-evaluation",
		Short:       "<p>Runs an on-demand evaluation for the specified resource to determine whether the resource details will comply...",
		Long:        "Shortcut for 'x-amz-target-starling-dove-service-start-resource-evaluation start-resource-evaluation'. <p>Runs an on-demand evaluation for the specified resource to determine whether the resource details will comply...",
		Example:     "  aws-pp-pp-cli x-amz-target-starling-dove-service-start-resource-evaluation --evaluation-mode example-value",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-starling-dove-service-start-resource-evaluation.start-resource-evaluation", "pp:method": "POST", "pp:path": "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.StartResourceEvaluation"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !cmd.Flags().Changed("evaluation-mode") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "evaluation-mode")
			}
			if !cmd.Flags().Changed("resource-details-resource-configuration") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "resource-details-resource-configuration")
			}
			if !cmd.Flags().Changed("resource-details-resource-id") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "resource-details-resource-id")
			}
			if !cmd.Flags().Changed("resource-details-resource-type") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "resource-details-resource-type")
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://config.us-east-1.amazonaws.com/#X-Amz-Target=StarlingDoveService.StartResourceEvaluation"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyClientToken != "" {
				body["ClientToken"] = bodyClientToken
			}
			{
				nestedEvaluationContext := map[string]any{}
				if bodyEvaluationContextEvaluationContextIdentifier != "" {
					nestedEvaluationContext["EvaluationContextIdentifier"] = bodyEvaluationContextEvaluationContextIdentifier
				}
				if len(nestedEvaluationContext) > 0 {
					body["EvaluationContext"] = nestedEvaluationContext
				}
			}
			if bodyEvaluationMode != "" {
				body["EvaluationMode"] = bodyEvaluationMode
			}
			if bodyEvaluationTimeout != 0 {
				body["EvaluationTimeout"] = bodyEvaluationTimeout
			}
			{
				nestedResourceDetails := map[string]any{}
				if bodyResourceDetailsResourceConfiguration != "" {
					nestedResourceDetails["ResourceConfiguration"] = bodyResourceDetailsResourceConfiguration
				}
				if bodyResourceDetailsResourceConfigurationSchemaType != "" {
					nestedResourceDetails["ResourceConfigurationSchemaType"] = bodyResourceDetailsResourceConfigurationSchemaType
				}
				if bodyResourceDetailsResourceId != "" {
					nestedResourceDetails["ResourceId"] = bodyResourceDetailsResourceId
				}
				if bodyResourceDetailsResourceType != "" {
					nestedResourceDetails["ResourceType"] = bodyResourceDetailsResourceType
				}
				if len(nestedResourceDetails) > 0 {
					body["ResourceDetails"] = nestedResourceDetails
				}
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyClientToken, "client-token", "", "Client token")
	cmd.Flags().StringVar(&bodyEvaluationContextEvaluationContextIdentifier, "evaluation-context-evaluation-context-identifier", "", "Evaluation context identifier")
	cmd.Flags().StringVar(&bodyEvaluationMode, "evaluation-mode", "", "Evaluation mode")
	cmd.Flags().IntVar(&bodyEvaluationTimeout, "evaluation-timeout", 0, "Evaluation timeout")
	cmd.Flags().StringVar(&bodyResourceDetailsResourceConfiguration, "resource-details-resource-configuration", "", "Resource configuration")
	cmd.Flags().StringVar(&bodyResourceDetailsResourceConfigurationSchemaType, "resource-details-resource-configuration-schema-type", "", "Resource configuration schema type")
	cmd.Flags().StringVar(&bodyResourceDetailsResourceId, "resource-details-resource-id", "", "Resource id")
	cmd.Flags().StringVar(&bodyResourceDetailsResourceType, "resource-details-resource-type", "", "Resource type")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
