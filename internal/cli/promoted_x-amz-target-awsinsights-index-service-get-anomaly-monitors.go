// Copyright 2026 zaydiscold. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newXAmzTargetAwsinsightsIndexServiceGetAnomalyMonitorsPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyMaxResults int
	var bodyMonitorArnList string
	var bodyNextPageToken string

	cmd := &cobra.Command{
		Use:         "x-amz-target-awsinsights-index-service-get-anomaly-monitors",
		Short:       "Retrieves the cost anomaly monitor definitions for your account. You can filter using a list of cost anomaly monitor...",
		Long:        "Shortcut for 'x-amz-target-awsinsights-index-service-get-anomaly-monitors get-anomaly-monitors'. Retrieves the cost anomaly monitor definitions for your account. You can filter using a list of cost anomaly monitor...",
		Example:     "  aws-pp-pp-cli x-amz-target-awsinsights-index-service-get-anomaly-monitors",
		Annotations: map[string]string{"pp:endpoint": "x-amz-target-awsinsights-index-service-get-anomaly-monitors.get-anomaly-monitors", "pp:method": "POST", "pp:path": "http://ce.us-east-1.amazonaws.com/#X-Amz-Target=AWSInsightsIndexService.GetAnomalyMonitors"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "http://ce.us-east-1.amazonaws.com/#X-Amz-Target=AWSInsightsIndexService.GetAnomalyMonitors"
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			body := map[string]any{}
			if bodyMaxResults != 0 {
				body["MaxResults"] = bodyMaxResults
			}
			if bodyMonitorArnList != "" {
				var parsedMonitorArnList any
				if err := json.Unmarshal([]byte(bodyMonitorArnList), &parsedMonitorArnList); err != nil {
					return fmt.Errorf("parsing --monitor-arn-list JSON: %w", err)
				}
				body["MonitorArnList"] = parsedMonitorArnList
			}
			if bodyNextPageToken != "" {
				body["NextPageToken"] = bodyNextPageToken
			}
			data, _, err := c.Post(path, body)
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().IntVar(&bodyMaxResults, "max-results", 0, "Max results")
	cmd.Flags().StringVar(&bodyMonitorArnList, "monitor-arn-list", "", "Monitor arn list")
	cmd.Flags().StringVar(&bodyNextPageToken, "next-page-token", "", "Next page token")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
