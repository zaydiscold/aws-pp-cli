version: 2
project_name: aws-pp-pp-cli
changelog:
  disable: true
builds:
  - id: aws-pp-pp-cli
    main: ./cmd/aws-pp-pp-cli
    binary: aws-pp-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X aws-pp-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: aws-pp-pp-mcp
    main: ./cmd/aws-pp-pp-mcp
    binary: aws-pp-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: aws-pp-pp-cli
    repository:
      owner: zaydiscold
      name: homebrew-tap
    homepage: "https://github.com/zaydiscold/aws-pp-pp-cli"
    description: "Combined CLI for multiple API services"
    install: |
      bin.install "aws-pp-pp-cli"
      bin.install "aws-pp-pp-mcp"
